{
  "name": "@piggy/email",
  "version": "1.0.0",
  "description": "Email service with Factory + Builder pattern for Piggy Parcel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "example": "node -r ts-node/register examples/usage-examples.ts"
  },
  "keywords": ["email", "factory", "builder", "templates"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "resend": "^6.0.2",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "@piggy/types": "workspace:*",
    "@piggy/typescript-config": "workspace:*",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@piggy/config": "workspace:*"
  }
}
